[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dino"
version = "0.0.0"
description = "Fork of facebookresearch/dino with packaging metadata."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }

[tool.setuptools]
packages = ["dino"]
package-dir = { "dino" = "." }
